# Demo Project Instructions

- Prefer small, pure functions
- Keep this package dependency-free (ES modules only)
- After code changes, run `npm start`
- Do not invent HTTP servers or packages that are not in package.json
