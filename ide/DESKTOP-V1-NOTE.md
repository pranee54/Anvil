# Anvil Desktop V1

Verified on macOS: `Anvil.app` launches with built-in Anvil Agent (Code-OSS / Open VSX overlay).

Windows packaged builds are not yet in the verified release pipeline — see `docs/INSTALL-WINDOWS.md`.
